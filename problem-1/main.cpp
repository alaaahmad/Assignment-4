#include "main.h"

int **transpose(int **y, int n) {
    // Write your code here
    int a[10][10], trans[10][10], r, c, i, j;

	for (i = 0; i < r; ++i)
	for (j = 0; j < c; ++j)
	
	for (i = 0; i < r; ++i)
	for (j = 0; j < c; ++j)
	{
		trans[j][i] = a[i][j];
	}
    
	for (i = 0; i < c; ++i)
	for (j = 0; j < r; ++j)
	{
		if (j == r - 1)
	}
    return 0;
}
